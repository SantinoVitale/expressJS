<div>
  {{#if h1title}}
  <h1 style="text-align: center;">{{h1title}}</h1>
  {{else}}
  <h1>UNKOWN TITLE</h1>
  {{/if}}
  {{#each products}}
  <p>nombre: {{this.title}}</p>
  <p>id: {{this._id}}</p>
  <p>precio: ${{this.price}}</p>
  <hr/>
  {{/each}}
</div>